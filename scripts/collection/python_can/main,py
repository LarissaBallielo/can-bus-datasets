
def main():
    pids = carregar_pids("pids.csv")
 
    with can.Bus(channel="can0", interface="socketcan") as bus:
        reader = ObdReader(bus, pids) 
        try:
            read_all()
        except KeyboardInterrupt:
            print("Encerrado.")
 
 
if __name__ == "__main__":
    main()